# AGENTS.md — Aniekan Udo Portfolio

## Project Overview

This is the automated portfolio site for **Aniekan Udo**, a Medical Student & AI Software Engineer specializing in Data, AI, Fullstack, MLOps, and Healthcare Technology. The site is built with Astro, styled with Tailwind CSS, and deployed to GitHub Pages via GitHub Actions.

**Key features:**
- **Projects** are automatically synced from GitHub repos listed in `projects.yaml`
- **Research** papers are auto-synced from repo READMEs following `RESEARCH_TEMPLATE.md`
- **Experience, Education, Skills, About** are config-driven via YAML files in `data/`
- **CI pipeline** fetches READMEs, thumbnails, research papers, and metadata at build time

**Sections:**
- **Hero** — Name, title, tagline
- **About** — Bio with photo
- **Skills** — Tech stack grid
- **Experience Timeline** — Professional journey with photos
- **Education** — Degrees and certifications
- **Research** — Auto-synced publications
- **Projects** — Filterable project grid

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Astro 5.x (static output) |
| Styling | Tailwind CSS 3.x |
| Fonts | Inter (body), Fira Code (mono) |
| CI/CD | GitHub Actions |
| Hosting | GitHub Pages |
| Automation | Python 3 + PyYAML (runs in isolated venv) |
| Package Manager | uv (Python), npm (Node) |

## Project Structure

```
.
├── .github/workflows/build-and-deploy.yml  # CI/CD pipeline
├── .venv/                                  # Python virtual environment (not committed)
├── data/                                   # YAML content configs
│   ├── about.yaml                          # Profile, social links, contact
│   ├── experience.yaml                     # Experience timeline entries
│   ├── education.yaml                      # Education cards
│   ├── research.yaml                       # Research papers (auto-sync + manual)
│   └── skills.yaml                         # Skills categories
├── scripts/
│   ├── sync-projects.py                    # Fetches repo data from GitHub
│   ├── sync-research.py                    # Parses research repo READMEs
│   ├── sync-content.py                     # Validates YAML and generates TypeScript
│   └── requirements.txt                    # Python dependencies (pyyaml)
├── github-profile-readme/                  # GitHub profile README assets
│   ├── README.md                           # Profile README code
│   ├── banner.svg                          # Animated header banner
│   └── INSTRUCTIONS.md                     # Setup instructions
├── projects.yaml                           # Single source of truth for showcased repos
├── src/
│   ├── content/config.ts                   # Astro content collections schema
│   ├── content/projects/                   # Generated at build time (*.md)
│   ├── content/research/                   # Generated by sync-research.py (*.md)
│   ├── data/content.ts                     # Generated by sync-content.py
│   ├── components/                         # Astro UI components
│   │   ├── Hero.astro                      # Landing hero section
│   │   ├── About.astro                     # Bio with photo
│   │   ├── Skills.astro                    # Tech stack grid
│   │   ├── Experience.astro                # Timeline + photos
│   │   ├── Education.astro                 # Education cards
│   │   ├── Research.astro                  # Featured publications
│   │   ├── ProjectCard.astro               # Individual project card
│   │   ├── ProjectGrid.astro               # Filterable project grid
│   │   ├── Navigation.astro                # Fixed navbar
│   │   └── Footer.astro                    # Contact + footer
│   ├── layouts/BaseLayout.astro            # Root HTML shell
│   ├── pages/
│   │   ├── index.astro                     # Landing page (all sections)
│   │   └── projects/[slug].astro           # Dynamic project detail pages
│   └── styles/global.css                   # Tailwind directives + custom utilities
├── public/
│   ├── profile.jpg                         # Author headshot
│   ├── default-thumbnail.svg               # Fallback for repos without images
│   ├── experience/                         # Experience photos
│   ├── education/                          # School logos
│   ├── research/                           # Research thumbnails
│   └── projects/                           # Downloaded repo thumbnails
└── astro.config.mjs                        # Astro config (static output)
```

## Development Workflow

### Prerequisites

- **Node.js** 18+ and **npm**
- **Python** 3.10+ and **uv** (`pip install uv` or `curl -LsSf https://astral.sh/uv/install.sh | sh`)

### Initial Setup

```bash
# 1. Install Node dependencies
npm install

# 2. Create Python virtual environment and install deps
uv venv .venv --python 3.12
uv pip install -r scripts/requirements.txt --python .venv\Scripts\python.exe

# 3. Sync content (generates src/data/content.ts)
npm run sync:content

# 4. Sync projects (generates src/content/projects/*.md)
npm run sync

# 5. Sync research (generates src/content/research/*.md)
npm run sync:research

# 6. Start dev server (localhost:4321)
npm run dev
```

### Daily Development

```bash
# Sync all content
npm run sync:content
npm run sync
npm run sync:research

# Start dev server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

### Python Environment Isolation

**Important:** All sync scripts run inside the `.venv` virtual environment.

- **Windows:** `.venv\Scripts\python.exe`
- **Linux/macOS:** `.venv/bin/python`

The `npm run` commands are pre-configured to use the correct path on Windows. On CI (GitHub Actions), it uses the Linux path automatically.

## Adding Content

### Projects

Edit `projects.yaml`:

```yaml
projects:
  - repo: Dr-Aniekan-Udo/your-new-repo
    category: "Fullstack"
    featured: true      # optional: highlights on grid
    priority: 1         # optional: sort order (lower = first)
```

**Project repo README:** Follow `PROJECT_README_TEMPLATE.md` for enhanced parsing.

Push to `main`. GitHub Actions will:
1. Create a fresh Python venv and install deps
2. Run `sync-projects.py` to fetch READMEs and thumbnails
3. Build the Astro site
4. Deploy to GitHub Pages

### CI/CD Schedule

The site rebuilds automatically on:
- **Every push to `main`** — immediate deployment
- **Monthly** — 1st of every month at midnight UTC (fetches latest repo data)
- **Manual trigger** — run anytime via GitHub UI, CLI, or API

#### Manual Build Trigger

**Via GitHub UI:**
1. Go to repository → Actions tab
2. Select "Build and Deploy" workflow
3. Click "Run workflow" → "Run workflow"

**Via GitHub CLI:**
```bash
gh workflow run build-and-deploy.yml
```

**Via API (requires token):**
```bash
curl -X POST \
  -H "Authorization: token YOUR_GITHUB_TOKEN" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/repos/Dr-Aniekan-Udo/dr-aniekan-udo.github.io/actions/workflows/build-and-deploy.yml/dispatches \
  -d '{"ref":"main"}'
```

### Research Papers

**Option A: Auto-sync from repo (recommended)**

1. Create a research repo with README following `RESEARCH_TEMPLATE.md`
2. Add to `data/research.yaml`:
```yaml
research:
  - repo: "Dr-Aniekan-Udo/your-research-repo"
    autoSync: true
    featured: true
    priority: 1
```

**Option B: Manual entry**
```yaml
research:
  - title: "Paper Title"
    authors: "Author 1, Aniekan Udo, Author 3"
    venue: "Conference Name"
    arxiv: "xxxx.xxxxx"
    abstract: "Abstract text..."
    methodology:
      - "Step 1"
      - "Step 2"
    conclusion: "Results..."
    tags: ["Tag1", "Tag2"]
    featured: true
    priority: 1
```

**Option C: Repo with manual override**
```yaml
research:
  - repo: "Dr-Aniekan-Udo/future-paper"
    autoSync: true
    featured: true
    priority: 3
    thumbnail: "/research/custom-thumb.jpg"
    venue: "Override venue"
```

### Experience

Edit `data/experience.yaml`:
```yaml
experience:
  - role: "New Role"
    org: "Organization Name"
    period: "Jan 2026 – Present"
    description: "What you did..."
    tags: ["Python", "AI"]
    featured: true
    photo: "/experience/new-role.jpg"  # Optional
    photoAlt: "Description of photo"
    priority: 1
```

Run `npm run sync:content` to regenerate.

### Education

Edit `data/education.yaml`:
```yaml
education:
  - degree: "New Certificate"
    school: "School Name"
    period: "2026"
    description: "What you learned..."
    logo: "/education/school-logo.png"  # Optional
    priority: 1
```

### Skills

Edit `data/skills.yaml`:
```yaml
skills:
  "New Category":
    - Skill 1
    - Skill 2
```

### Profile / About

Edit `data/about.yaml`:
```yaml
profile:
  name: "Your Name"
  title: "Your Title"
  tagline: "Tagline"
  bio: "Your bio..."
  photo: "/profile.jpg"

social:
  github: "username"
  linkedin: "linkedin-url"
  twitter: "twitter-handle"
  email: "your+alias@gmail.com"
```

## Content Config Files

### Project Categories

Use one of these categories for consistency:
- `Fullstack`
- `AI & Agents`
- `MLOps & Tools`
- `Machine Learning`
- `Data Science`

### Thumbnail Discovery Priority

For projects, the script searches in this order:
1. `thumbnail.{png,jpg}` (repo root)
2. `assets/thumbnail.{png,jpg}`
3. `images/thumbnail.{png,jpg}`
4. `assets/preview.{png,jpg}`
5. `docs/cover.{png,jpg}`
6. `.github/preview.{png,jpg}`
7. `screenshot.{png,jpg}`
8. `banner.{png,jpg}`

If none found → `default-thumbnail.svg`

### Thumbnail Specifications

| Section | Recommended Size | Aspect Ratio | Format |
|---------|------------------|--------------|--------|
| **Projects** | 1280×720px | 16:9 | PNG or JPG |
| **Research** | 1200×630px | 1.91:1 | PNG or JPG |
| **Experience** | 800×600px | 4:3 | PNG or JPG |
| **Education logos** | 400×400px | 1:1 | PNG with transparency |

## Automation Details

### `sync-projects.py`

1. Reads `projects.yaml`
2. For each repo:
   - Fetches GitHub API metadata (stars, language, topics, description)
   - Fetches `README.md` from `main` branch (falls back to `master`)
   - Extracts first paragraph or `## Overview` section as excerpt
   - Rewrites relative image paths to absolute GitHub raw URLs
   - Searches for thumbnail in common paths
   - Falls back to `public/default-thumbnail.svg`
3. Generates `src/content/projects/{slug}.md` with frontmatter + full README

### `sync-research.py`

1. Reads `data/research.yaml`
2. For each entry with `autoSync: true`:
   - Fetches `README.md` from repo
   - Parses structured content (Authors, Venue, arXiv, Abstract, Methodology, etc.)
   - Downloads thumbnail if specified
   - Generates `src/content/research/{slug}.md`
3. For manual entries, generates markdown from YAML data

### `sync-content.py`

1. Reads all YAML files from `data/`
2. Validates schema (skips invalid entries with warnings)
3. Generates placeholder SVGs for missing photos/logos
4. Outputs `src/data/content.ts` — typed TypeScript data for components

## Agent Workflow: Repo Analysis with gitingest

When improving project READMEs or analyzing repositories, **always use gitingest** for comprehensive codebase understanding before making changes.

### Why gitingest?

- Extracts full repository structure, file contents, and dependencies
- Provides complete context for accurate README generation
- Reveals architecture, features, and tech stack automatically
- Much more reliable than manual browsing or GitHub API alone

### Installation

```bash
# gitingest is already installed in .venv
uv pip install gitingest
```

### Usage

```python
from gitingest import ingest

# Analyze any public repo
summary, tree, content = ingest('https://github.com/username/repo-name')

# Save for analysis
with open('repo_digest.txt', 'w', encoding='utf-8') as f:
    f.write(f'Summary: {summary}\n\nTree:\n{tree}\n\nContent:\n{content[:50000]}')
```

### Agent Workflow

1. **Ingest** the target repo using gitingest
2. **Analyze** the output to understand:
   - Project structure and architecture
   - Key features and capabilities
   - Tech stack and dependencies
   - Code organization patterns
3. **Generate** improved README following `PROJECT_README_TEMPLATE.md`
4. **Preserve** all embedded images and screenshots
5. **Clean up** all temporary digest files before committing:
   ```bash
   # Remove all gitingest output files
   Remove-Item -Path "*digest*","*gitingest*","*.tmp" -ErrorAction SilentlyContinue
   ```

### Important Rules

- **Never commit** gitingest output files (`*digest*`, `*.tmp`, temp `.txt` files)
- Always clean up temporary files after analysis is complete
- Use the full content output for comprehensive understanding
- Cross-reference with existing README to preserve embedded images

## Design System

### Colors

| Token | Hex | Usage |
|-------|-----|-------|
| `dark` | `#0a0a0a` | Page background |
| `dark-surface` | `#141414` | Cards, sections |
| `dark-elevated` | `#1a1a1a` | Hover states, badges |
| `amber` | `#f59e0b` | Primary accent, links, CTAs |
| `amber-hover` | `#d97706` | Button hover |
| `text-primary` | `#e5e5e5` | Headings |
| `text-secondary` | `#a3a3a3` | Body text |
| `text-muted` | `#737373` | Captions, labels |
| `border` | `#262626` | Dividers, card borders |

### Animation

- **Scroll reveals:** `.reveal` class + IntersectionObserver (0.6s ease-out)
- **Stagger grids:** `.stagger-children` class (100ms increments)
- **Hover effects:** Cards lift `-translate-y-1`, border shifts to `amber/50`

## Troubleshooting

### Build fails locally

```bash
# Ensure venv exists and has deps
uv venv .venv --python 3.12
uv pip install -r scripts/requirements.txt --python .venv\Scripts\python.exe

# Sync all content
npm run sync:content
npm run sync
npm run sync:research
```

### Build fails on GitHub Actions

- Check `projects.yaml` and `data/*.yaml` syntax is valid
- Ensure repos in whitelist are public
- Verify `GITHUB_TOKEN` has sufficient permissions (read repos)

### Thumbnails not appearing

- Add a `thumbnail.png` or `thumbnail.jpg` to the repo root or `assets/` folder
- For research repos, specify `**Thumbnail:** assets/thumbnail.jpg` in README
- Run `npm run sync` or `npm run sync:research` to re-fetch

### Local dev errors

- Run `npm run sync:content` first — components need `src/data/content.ts`
- Run `npm run sync` — Astro content collections need the `.md` files
- Ensure Node 18+ and Python 3.10+ are installed
- Ensure `.venv` exists with `pyyaml` installed

### Content not updating

- Edit the YAML file in `data/`
- Run `npm run sync:content`
- Restart dev server if needed

## Contact

Maintained by Aniekan Udo. For issues, open a GitHub issue or email aniekanetimudo+reachout@gmail.com.
